# Local Growth Photos

BabyLog stores uploaded growth photos in this directory using generated filenames.
SQLite stores only the filename in `measurements.photo_filename`.

These files are private baby data. Back them up with `babylog.db`, and do not publish
this directory to a public repository or web server.
